Установите зависимости:

```bash
npm install
# или
yarn install
```

В корне проекта создайте файл `.env` и добавьте в него переменную:

```bash
REACT_APP_API_URL=
```

Запустите бота:

```bash
npm run start
# или
yarn run start
```
